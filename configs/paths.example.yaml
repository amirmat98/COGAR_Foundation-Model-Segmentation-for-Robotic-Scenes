# Copy this file to configs/paths.yaml and adjust paths for your machine.

project_root: "."

# Full OCID dataset stored outside Git or under the ignored local /datasets root.
ocid_root: "datasets/OCID-dataset"

# First sequence used by the OCID debug pipeline.
ocid_debug_sequence: "YCB10/table/top/mixed/seq21"

rgb_folder_name: "rgb"
label_folder_name: "label"

# Local outputs inside the repo, ignored by Git except outputs/README.md.
outputs_dir: "outputs"
indexes_dir: "outputs/indexes"
gt_binary_masks_dir: "outputs/gt_binary_masks"
sam_outputs_dir: "outputs/sam_box_prompt"
