task:
  name: task5_classical_baselines
  description: small-subset supervised baselines for robotic segmentation
  output_root: outputs/task5_baselines
  data_root: data/task5_baselines
  seed: 5884715

split_policy:
  $ref: common.yaml#/task5/split_policy

datasets:
  $ref: common.yaml#/datasets

baselines:
  yolo8_seg:
    enabled: true
    format: yolo_segmentation
    image_strategy: symlink
    min_contour_points: 3

  mask_rcnn:
    enabled: true
    format: coco_instance

  deeplabv3plus:
    enabled: true
    format: semantic_masks
    background_label: 0
